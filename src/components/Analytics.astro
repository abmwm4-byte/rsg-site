---
// Счётчики аналитики. Каждый включается независимо, как только подставлен реальный ID.
const YM_ID = '62982700'; // Яндекс Метрика, Вебвизор включён
const GA_ID = 'G-XXXXXXXXXX'; // TODO(analytics): GA4, когда будет создан ресурс
const ymEnabled = !YM_ID.startsWith('0000');
const gaEnabled = !GA_ID.startsWith('G-XXXX');
---

{ymEnabled && (
  <script is:inline define:vars={{ YM_ID }}>
    (function(m,e,t,r,i,k,a){m[i]=m[i]||function(){(m[i].a=m[i].a||[]).push(arguments)};
    m[i].l=1*new Date();k=e.createElement(t),a=e.getElementsByTagName(t)[0],
    k.async=1,k.src=r,a.parentNode.insertBefore(k,a)})
    (window,document,'script','https://mc.yandex.ru/metrika/tag.js','ym');
    ym(YM_ID,'init',{clickmap:true,trackLinks:true,accurateTrackBounce:true,webvisor:true});
  </script>
)}
{gaEnabled && (
  <>
    <script is:inline async src={`https://www.googletagmanager.com/gtag/js?id=${GA_ID}`}></script>
    <script is:inline define:vars={{ GA_ID }}>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', GA_ID);
    </script>
  </>
)}

<!-- Цели: клики по звонкам и мессенджерам (data-goal на ссылках) -->
<script is:inline define:vars={{ YM_ID }}>
  document.addEventListener('click', (e) => {
    const el = e.target.closest('[data-goal]');
    if (!el) return;
    const goal = el.dataset.goal;
    if (typeof ym !== 'undefined') ym(YM_ID, 'reachGoal', goal);
    if (typeof gtag !== 'undefined') gtag('event', goal, { event_category: 'contact' });
  });
</script>
